Error: (pymysql.err.ProgrammingError) (1064," ""You have an error in your SQL syntax; check the manual that corresponds to your MySQL server version for the right syntax to use near 'FULL OUTER JOIN user_blog_stats ubs ON ufc.user_id = ubs.user_id\n)","\ncorrelation_' at line 25"")"
[SQL: WITH user_follower_counts AS (
    SELECT 
        follow_user_id AS user_id,
        COUNT(*) AS follower_count
    FROM tb_follow
    GROUP BY follow_user_id
),
user_blog_stats AS (
    SELECT 
        b.user_id,
        COUNT(*) AS blog_count,
        AVG(b.liked) AS avg_liked,
        AVG(b.comments) AS avg_comments
    FROM tb_blog b
    GROUP BY b.user_id
),
user_metrics AS (
    SELECT 
        COALESCE(ufc.user_id, ubs.user_id) AS user_id,
        COALESCE(ufc.follower_count, 0) AS follower_count,
        COALESCE(ubs.blog_count, 0) AS blog_count,
        COALESCE(ubs.avg_liked, 0) AS avg_liked,
        COALESCE(ubs.avg_comments, 0) AS avg_comments
    FROM user_follower_counts ufc
    FULL OUTER JOIN user_blog_stats ubs ON ufc.user_id = ubs.user_id
),
correlation_data AS (
    SELECT 
        follower_count,
        avg_liked AS liked
    FROM user_metrics
    WHERE follower_count > 0 AND avg_liked > 0
),
avg_blog_count_by_follower_group AS (
    SELECT 
        CASE WHEN follower_count > 5 THEN 'gt_5' ELSE 'le_5' END AS group_name,
        AVG(blog_count) AS avg_blog_count
    FROM user_metrics
    GROUP BY CASE WHEN follower_count > 5 THEN 'gt_5' ELSE 'le_5' END
)
SELECT 
    (SELECT COUNT(*) FROM tb_follow f JOIN tb_blog b ON f.follow_user_id = b.user_id) AS total_follow_blog_links,
    (SELECT COUNT(*) FROM tb_follow f JOIN tb_blog b ON f.follow_user_id = b.user_id JOIN tb_blog_comments bc ON b.id = bc.blog_id) AS total_follow_blog_comment_links,
    (SELECT 
         (COUNT(*) * SUM(x.y) - SUM(x.x) * SUM(x.y)) / 
         SQRT((COUNT(*) * SUM(x.x * x.x) - SUM(x.x) * SUM(x.x)) * (COUNT(*) * SUM(x.y * x.y) - SUM(x.y) * SUM(x.y)))
     FROM (
         SELECT follower_count AS x, liked AS y FROM correlation_data
     ) x) AS corr_liked_follower_count,
    (SELECT AVG(avg_blog_count) FROM avg_blog_count_by_follower_group WHERE group_name = 'gt_5') AS avg_blog_count_gt_5,
    (SELECT AVG(avg_blog_count) FROM avg_blog_count_by_follower_group WHERE group_name = 'le_5') AS avg_blog_count_le_5;]
(Background on this error at: https://sqlalche.me/e/20/f405)
